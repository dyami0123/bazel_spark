build --spawn_strategy=standalone
test --spawn_strategy=standalone
run --spawn_strategy=standalone

# Pass host PATH so genrules can find conda, python, etc.
build --action_env=PATH
test --action_env=PATH